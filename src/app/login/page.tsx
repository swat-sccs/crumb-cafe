'use client';
import React from 'react';
import { TextField, Button, Typography, Grid, Card, Container } from '@mui/material';
import styles from './page.module.css';

const YourComponent = () => {
  return (
    <Container>
      <Card>
        <Typography variant="h2">Login!</Typography>
      </Card>
    </Container>
  );
};

export default YourComponent;
